# CST Agent 方案与审查入口

文档包版本：**0.2.0 / R1 / REVIEWED_CONDITIONAL**
编制日期：2026-09-20  
R0：ChatGPT；R1技术核验与修订：Codex；下一轮：基于实际提交复核；最终范围确认：项目负责人。

> 这是设计提案，不是实现完成声明，也不自动授权开发、安装、模型调用或 CST 求解。本轮只编写和审查文档。

## 阅读顺序

1. [整体架构与交付方案](architecture-and-delivery-plan.md)：目标、需求、结构、代码与 MCP 分工、3D/原理图、数据、运行、安全、测试、里程碑。
2. [功能覆盖与验收矩阵](capability-matrix.md)：以用户操作分类，逐步覆盖界面功能；不能用工具数量代替覆盖率。
3. [决策、问题与审查记录](review-log.md)：记录哪些建议被接受、修改、反对，以及依据。
4. [Codex 本轮审查任务](CODEX_REVIEW_TASK.md)：从本地现状核验方案，直接修订本套文件，再提交复核。

## 核心方向

**Pi Agent + PiDeck + 现有 CST MCP；代码优先，可靠封装复用，统一执行和验证。**

Pi 负责需求理解、必要补问、查询资料、编写/修改 Python 或 VBA、解释依据。MCP 是接入通道，不是智能体本身；其后端负责工程身份、权限、执行、状态、读回、日志和结果。CST 保留几何、原理图、网格、求解和后处理职责。PiDeck 展示实际工程的可视化副本，不另建竞争模型真源。

长期追求广泛功能覆盖，短期同时验证一个3D小案例和一个原理图小案例。两者都纳入架构，不把原理图推迟到天线平台做完之后。CST与FDTD隔离开发，复用明确版本的PiDeck代码和交互经验，不改其生产目录和全局配置。

**R1总评：有条件接受，建议先冻结P1A范围，不一次冻结完整后台求解和全界面替代承诺。** 第一包收紧为工程绑定、明确执行域、双域实际读回和保存恢复；真取消在紧随其后的P1B独立技术门验证。源码存在、官方帮助、示例缓存、历史实机记录和本轮测试分别计证据。

## 当前状态

| 项目 | 状态 |
|---|---|
| 已核验的远端代码基线 | `main @ 14b7f89d28a15b8c64af5c511d95bce1307a60ea` |
| 方案工作分支 | `docs/cst-agent-architecture-review-20260920` |
| R1文档输入HEAD | `209828e58b911d542e302cfb0e3b7d7fd9dffdbd`；fetch后确认当时为最新，并在独立detached worktree审查 |
| 本地源码与增强 | main基线相同；5个源码/4个测试修改及1个未跟踪测试已只读核对并保护；177项远端/183项本地静态工具声明，非覆盖率 |
| 本机版本资料 | CST2025帮助已读；当前运行build/许可证额度未探测；CLI包0.85.1、参考Host SDK0.80.10；适配器源码2.34.0，不等于已安装/加载 |
| R1方案 | 已完成源码、官方帮助和既有证据审查；保留条件性技术门，详见Q-01至Q-12结论 |
| 本轮验证 | 仅文档结构/链接/编号/敏感内容/差异范围及保护检查；不运行pytest、MCP服务、CST、产品Provider或数值实验 |
| 下一步 | 复核R1提交并批准首包范围/预算后再实施；本次文档提交不构成运行授权 |

该基线不是对本地工作区版本的推断。历史会话中记录过本地增强，必须保护并单独核验。后续分支有新提交时，使用最新 HEAD 并记录，不回退至 R0。

## R1最重要的修正

1. **复用边界落到文件和版本。** 原理图RLC/Net早已存在；瞬态任务、波形和网格增强仍在本地。以单一CST后端管理写锁/收据；PiDeck按钮与Pi最终接同一后端，不各开一个stdio进程。保留代码扩展出口，首包不建设任意Python沙箱或大代理SDK。
2. **运行与测试不再混用标签。** 当前服务启动就connect，必须先提供显式no-connect协议模式；3D求解阻塞与DS Update执行子任务分别处理。请求取消、实际停止、结果采集失败和未知状态分开，先做P1B实机技术验证再承诺无人值守。
3. **实际状态与证据分层。** 参数MsgBox、字符串结果和设置回显不等于结构化读回；官方缓存日志不算当前MCP验收。3D显示面片、真实网格、原理图拓扑/派生布局、DS与3D结果树各有契约。

## 第一交付包及未决项

[CST-1/P1A](architecture-and-delivery-plan.md#141-推荐第一个交付包cst-1p1a-双域绑定与读回)：一个PEC砖参数修改案例＋一个RLC/ExternalPort/Ground网络案例，验证绑定、表达式/单位/实际属性、连接、保存重开、重复/过期请求和部分失败。**不含求解、Provider或完整PiDeck UI**，但两个域都必须给可读回证据。

预算建议：2–4个开发工作日；未来获准L2后最多4次专用实例启动、两域各≤3次完整尝试、每次≤5分钟、实机窗口≤30分钟；求解/网格/Provider均0。资源监控阈值和停止条件见主方案，不是本轮执行额度。随后P1B才提出微型求解/取消预算。

剩余技术门：DS具体属性/参考节点读回、3D及DS阻塞期间停止、工程依赖快照与外部未保存修改、CST显示/结果导出与实际适配器加载。原截图未随文档提供，R1保留全部56项转录但不声称原图完整性已验。上述远期未知不阻止首包设计冻结；首包自身读回缺口必须在交付前解决或明确阻断，不能降验收门。

## 更新规则

主方案始终维护同一路径，不创建多个同时声称“最新”的总方案。每轮在 `review-log.md` 追加记录，标明审查输入提交、文档版本、依据、修改和未决事项。旧文本由 Git 历史保留，失败/实验原始证据不得覆盖。聊天用于传达结论，不能替代仓库中的设计与证据。

建议循环：**ChatGPT R0 → Codex 核验并修订 R1 → ChatGPT 核验提交并复核 R2 → 项目负责人确认设计基线 → 分里程碑实施。** 这是人工触发的交接，不是已经安装的自动审查服务。

只有影响核心可行性、安全边界或当前交付范围的未决问题阻止设计冻结；远期能力和必须通过实机验证的问题进入对应里程碑，不无限延长纸面讨论。审查一致不等于功能通过。

## 公开仓库边界

本仓库在 R0 核验时为公开仓库。仅提交可公开的原创方案、接口设计和脱敏记录。不上传凭据、个人路径、原始会话、私有项目内容、商业 CST 工程、受限数据、CST 安装库或完整官方帮助。已有开源许可和第三方声明保持不变；软件发布与本轮设计文档提交是两件事。
