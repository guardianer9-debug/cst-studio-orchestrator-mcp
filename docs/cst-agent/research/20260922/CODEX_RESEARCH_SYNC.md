# Codex：CST论文研究同步与下一步审查

日期2026-09-22。交给现有CST项目/会话，不交给FDTD合并会话。

## 目标

将本次12篇近邻核验并入已有文献库，结合实际代码独立审查候选创新，给出一页用户能看懂的研究方案与可实施的小规模实验计划。不是只润色，也不是立即重写软件或跑整批实验。

先读本目录README、related-work-and-novelty.md及两个BibTeX，再读当前CST总方案、功能矩阵、交付台账和本地原始证据。下载包中的扩展笔记/JSON可作为补充，本目录足以启动同步。

## 1. 保护工作区、同步文献

先fetch核对最新实现/文档HEAD及dirty，保护未提交改动。使用独立文档worktree或安全集成方法，不把文档旧代码覆盖生产，不force push，不合并main。

检查现有本地literature目录中的README、papers.json、references.bib、published-only.bib、逐篇笔记、研究结论和原始工程索引。旧记录报告29篇，先核实，不把本包12篇都当新增。

按DOI及arXiv去重，正式发表版优先，保留实际读过的版本。旧引用键尽量不改，必要时保存新旧键表。保留旧笔记、检索失败和否定结论；缺全文、卷期或页码不补造。

本包是研究快照，本地旧库仍是长期文献入口。仅提交原创分析、可公开元数据和脱敏记录，不提交个人路径、原始会话、受限工程或论文全文。

## 2. 独立审查最近邻与候选贡献

重点核对LADS、MetaDataGenAgent、AutoRF、HALO、JutulGPT、SchGen及验证/回归选择文献。区分论文主张、源码边界与本机实测；不因旧MetaDataGenAgent代码快照限制就否认论文求解/器件验证。

考察“人工/Agent修改中的场路一致性核验”是否真有增量：目标对象对应、端接/参考节点/观测含义、允许修改与必须保持的关系、判据适用性、错误定位和未知状态。图、读回、目录和版本管理不能单独写成首创。

你可以反对ChatGPT提案并给出更简单方案。固定检查足够就不强加LLM选择器，不新增固定多Agent、DSL或大型知识平台。不强制安装或复跑LEAM/HFSS/光子系统；先比较文献方法，实际外部系统复现另定范围。

## 3. 核对现有代码差距

列“已经实现、真正缺的方法、仅缺实验”三列。特别核对产品Pi是否真的加载和使用了开发时参考资料，不能拿Codex表现代替产品能力。

优先沿用已有偶极子、222及简单串扰候选，不转向用户不熟悉的器件。用户工程检查认可不升级为数值或泛化验收。

准备案例卡和预期关系：正确修改、错误目标/端接、合法改名/单位表示、探针和结果引用等。按结构分开开发与留出任务；只读利用现有证据，缺失部分明确待补。

## 4. 给出小规模公平实验计划

至少规划普通Pi＋同工具、固定完整检查、确定性依赖选择、候选方法。说明共同工具/资料和预算、增加的机制、消融、独立真值及误放行/误报/完成率/人工干预/成本指标。

不以方法自己的PASS或同一个LLM评分作唯一真值。不删除网络失败；真实错误与注入错误分别统计。写明预试验规模、重复与资源建议，以及什么结果会否定创新主张。

当前本轮不改生产代码、不安装升级、不合并CST/FDTD、不恢复新网格/task/求解、不提高222预算、不新增Provider批实验。可编写文献、案例说明、字段契约和实验规格。实机才能确定的事项标后续验证，集中提出，不让用户逐小步决定测试。此处不扩大或重置其他产品任务的既有授权。

## 5. 交付与回执

将接受的取舍更新到现有研究结论、唯一总方案相关章节和简化入口；候选机制标未验证。保留本轮快照与反对意见，不创建第二套最新总方案。

在文档工作分支提交推送可公开修改并更新已有本地固定阅读入口。

最后简单回答：别人做到了什么；我们想解决哪个具体错误；当前代码可复用什么；还需补什么；下一步最小实验是什么；用户在哪里看文献与方案。返回分支与完整commit SHA。

先把问题、增量和实验说清楚，再按后续批准范围实施，不能为了迎合建议宣布创新已经成立。
